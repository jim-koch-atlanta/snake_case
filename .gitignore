# Secrets / private league data (never commit)
.env
docs/league-config.toml
docs/player-notes.md

# Python bytecode
__pycache__/
*.pyc

# Tooling caches / venv (uv, pytest, ruff self-ignore too, but be explicit)
.venv/
.pytest_cache/
.ruff_cache/

# Do not upload other site's data
data/

# Throwaway ESPN poller dumps (--dump). Rest of data/ IS tracked (crosswalk etc.)
data/
data/draft_snapshots/
